import React from "react";
import ReactDOM from "react-dom/client";
import SurveyApp from "../SurveyApp";
import "../styles/lavallejaFilmaSurvey.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <SurveyApp />
  </React.StrictMode>
);
